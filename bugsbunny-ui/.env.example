# Copy this to .env (local) or set as an environment variable in your
# hosting provider's dashboard (Vercel/Netlify/Render) when deploying.
# Must point at wherever the Go backend is actually reachable.
VITE_API_URL=http://localhost:8080
